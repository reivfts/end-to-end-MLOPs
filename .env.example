# Campus Services Hub - Environment Configuration
# Copy this file to .env and update values for your environment

# ===========================================
# Flask Application Configuration
# ===========================================
FLASK_ENV=development
FLASK_DEBUG=1

# ===========================================
# JWT Authentication
# ===========================================
# CRITICAL: Change this in production!
JWT_SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ===========================================
# Database Configuration
# ===========================================
# Options: sqlite, postgresql
DATABASE_TYPE=sqlite

# PostgreSQL Configuration (for Docker/AWS)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campus_services
DB_USER=postgres
DB_PASSWORD=postgres

# SQLite Configuration (for local development)
SQLITE_DB_PATH=.

# ===========================================
# Service URLs (for Docker Compose)
# ===========================================
# Leave as localhost for local development
# Docker Compose will override these automatically

GATEWAY_HOST=localhost
GATEWAY_PORT=5001

USER_MGMT_HOST=localhost
USER_MGMT_PORT=8002

BOOKING_HOST=localhost
BOOKING_PORT=8001

GPA_HOST=localhost
GPA_PORT=8003

NOTIFICATION_HOST=localhost
NOTIFICATION_PORT=8004

MAINTENANCE_HOST=localhost
MAINTENANCE_PORT=8080

# ===========================================
# HTTP Client Configuration
# ===========================================
REQUEST_TIMEOUT=10
REQUEST_RETRY_ATTEMPTS=3
REQUEST_RETRY_BACKOFF=1.0

# ===========================================
# AWS Deployment (Production)
# ===========================================
# Set these for AWS ECS/EKS deployment
# DEPLOYMENT_ENV=aws
# AWS_REGION=us-east-1
# DB_HOST=your-rds-endpoint.rds.amazonaws.com
# DB_PASSWORD=your-secure-rds-password
