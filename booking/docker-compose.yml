version: '3.8'

services:
  booking-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./rooms.db:/app/rooms.db
    environment:
      - PYTHONUNBUFFERED=1
    container_name: booking-backend
    restart: unless-stopped
